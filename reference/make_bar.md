# Visualize categorical variables

This functions generates barplots for each categorical variable

## Usage

``` r
make_bar(df_bar)
```

## Arguments

- df_bar:

  A wide-format data include all categorical variables to visualize

## Value

Barplots in categorical variable tab

## Details

This function only run internally in VisXplore application because it
depends on reactive values from user input
