% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coradar.R
\name{plot.visx_coradar}
\alias{plot.visx_coradar}
\title{Plot method for visx_coradar objects}
\usage{
\method{plot}{visx_coradar}(
  x,
  sd_band = TRUE,
  band_width = 1,
  individuals = NULL,
  highlight = NULL,
  label_size = 4,
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{a visx_coradar object}

\item{sd_band}{if TRUE (default), shade a band of \code{mean +/- band_width * sd / 2}
around each archetype}

\item{band_width}{multiplier for the width of the variability band (default 1)}

\item{individuals}{optional individual observations to overlay as black
polygons: either row indices into the (complete-case) data, or a data.frame
of raw values containing the plotted variables (normalized to the scale of
the original data)}

\item{highlight}{optional character vector of variable names whose axes are
emphasized; remaining axes and labels are greyed out}

\item{label_size}{size of the axis labels (default 4)}

\item{legend}{if TRUE, show the archetype legend when groups are present}

\item{...}{additional arguments (ignored)}
}
\value{
a ggplot object, invisibly
}
\description{
Draws the co-radar plot: axes placed by correlation structure,
one mean polygon per archetype, and (optionally) a shaded band showing
within-group variability that fades with distance from the mean shape.
}
\examples{
result <- coradar(mtcars, vars = c("mpg", "disp", "hp", "drat", "wt", "qsec"))
plot(result, individuals = 1)
plot(result, highlight = c("mpg", "wt"))

}
